"""Optional transport adapters. The registry (registry.py) is authoritative and
fully functional without any of these -- same invariant as policy-registry
(D-20260728-001: "system-gap-master is only an optional transport adapter")."""
